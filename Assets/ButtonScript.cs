using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Assertions.Must;

public class ButtonScript : MonoBehaviour
{

    /*
    public BoxCollider[] colliders;

    private BoxCollider _left;
    private BoxCollider _down;
    private BoxCollider _up;
    private BoxCollider _right;*/
    // Start is called before the first frame update
    void Start()
    {/*
        _left = colliders[0];
        _down = colliders[1];
        _up = colliders[2];
        _right = colliders[3];*/
    }

    // Update is called once per frame
    void Update()
    {/*
        _left.enabled = Input.GetKey(KeyCode.LeftArrow);
        _down.enabled = Input.GetKey(KeyCode.DownArrow);
        _up.enabled = Input.GetKey(KeyCode.UpArrow);
        _right.enabled = Input.GetKey(KeyCode.RightArrow);*/
    }
    
}
